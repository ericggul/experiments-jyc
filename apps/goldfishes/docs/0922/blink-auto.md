# 0922 blink-auto

- Route: `/screen/0922/blink-auto` (legacy alias: `/0922/blink-auto`)
- Date: 2026-09-23
- Baseline: [`0922/default`](./default.md), at its accepted 80-source automatic-blink state

Question: can the accepted full-tech-eye automatic blink remain a fixed,
addressable baseline while a later route tests participant-triggered blinking?

This is a complete local snapshot made before the manual `blink-all` mutation.
It preserves the initially enabled `blinking 2D` mode, its 206–244 ms
independently timed eyelid motion, the static original-photo fallback, and all
existing field geometry, fish, presets, controls, colour treatment, and
reduced-motion behavior. The immutable 80 WebP blink sheets at
`/images/0922/tech-eye-blink/` remain shared by URL. The copied pond recording
is byte-identical but lives at the `blink-auto` audio URL. Session storage,
renderer labels, generation-ledger destination, and copied-module documentation
use the `blink-auto` identity.

The copied 3D-eye trial files are retained without a redesign or a new runtime
claim. Their rejected-source iris atlas is preserved locally at
`/images/0922/blink-auto/iris-atlas.png`, so this route does not depend on the
removed shared 3D path. This snapshot adds no observation beyond the accepted
`0922/default` review; its role is to preserve that reviewed automatic behavior
for direct comparison with the subsequent manual-blink experiment.
