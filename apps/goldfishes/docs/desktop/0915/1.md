# Desktop / 0915 / 1 — preserved configurable baseline

`/desktop/0915/1`. Complete copy of the current configurable desktop trial, made before version 2 changes. Own controller, settings, server, runner, Preview adapter and tests under `components/desktop/0915/1/`. Only endpoint and relative asset/runner paths changed to make the copy independently addressable. No imports from version 2 or the former flat implementation.

Retains selected apps, fixed/seeded-random order, transition interval/count, countdown, timing jitter, jump-position range and window size. Each run freezes settings. Original flat code and `0915/a` are retained as historical material; `0915/a/Untitled` was not touched.

Evidence inherited: typecheck and pure settings/admission tests passed. Prior native runs validated the earlier fixed score only. Preview permission correction and configurable browser execution were not visually verified before preservation; do not silently describe them as solved. [Earlier record](../../pc/0915-desktop-a.md).
