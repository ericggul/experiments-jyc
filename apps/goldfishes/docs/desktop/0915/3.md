# 0915 / 3 — Return and interrupt

Route: `/desktop/0915/3`. The user consolidated the former 4 into 3; continue editing this version unless explicitly asked to fork. Versions 1–2 are unchanged. Listed in home/Desktop navigation.

Question: can returning to an already open page interrupt the sequence of loading screens without abandoning new-window accumulation?

Terminal independently interrupts with a fixed 10% probability per event, using `open -a Terminal`: open or foreground the app without typing commands or accumulating new shell windows on every hit. Browser actions and optional Slack remain unchanged.

After three creation events, the default score chooses 40% random revisits and 60% creation. The interface exposes the revisit probability (0–100%). Creation still independently chooses a new window or tab. Ratios are probabilities, not exact quotas. Existing managed tabs are inventoried each event; the previous selected tab is excluded from revisit candidates. A missing pool falls back to creation. Revisits activate the exact tab ID without navigation, reload, creation or FIFO retirement; scroll can act on the selected tab. Loading completion is not guaranteed or awaited. No unrelated personal tabs are adopted.

Settings are frozen on Execute. Requested cadence remains 0.3 seconds, subject to the inherited count/latency backpressure. The 50-page limit, exact owned-tab FIFO closure, recent-window motion, optional Slack interruption and no-posting boundary remain. This version retains the internal `ownership4` ledger key to preserve tracking across the rename. Server restarts lose the ledger, leaving prior tabs unmanaged.

Static checks cover warmup, ratio extremes, deterministic plans, empty/one-tab revisit pools, exclusion of the previous tab, ID-only focus commands, ownership and existing geometry/security behavior. Native AppleScript execution and the perceived loading/revisit rhythm still need a live user test. No server start or live browser run was performed for this revision.
