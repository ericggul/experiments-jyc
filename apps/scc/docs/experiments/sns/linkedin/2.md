# LinkedIn 2

Experimental local reconstruction at `/sns/linkedin/2`, created 2026-09-13. `/1` remains unchanged.

The supplied desktop screenshot establishes the initial feed, profile, puzzles and recommendations. Signed-in Chrome observations establish notification filters/menus, navigation and composer appearance. Layout uses a 1128px workspace with 216/552/312px columns and 24px gaps. Screenshot content crops and captured source assets live in `public/images/sns/linkedin/2/`; interface controls are real DOM.

The feed contains three reference posts plus 24 fictional Korean/English professional posts and 18 seeded comments. Additional portraits/media reuse the established `/1` Unsplash sources. Generated posts are not statements by the real reference authors.

Local interactions include publishing, media attachments, comments, reactions, repost state, saving, sharing into local messages, sorting, search, notification filters/deletion/undo/preferences, and invitation actions. Jobs retain the isolated `/1` job implementation. Secondary service links open LinkedIn. This is not an authenticated backend replay: scheduling is unavailable, source video is a still, and full secondary-page/visual parity is not established.

Prior to the user stopping verification, scoped lint/typecheck passed and Chrome checks exercised publishing, comments, likes, notification delete/undo/filtering, saving, local messages, invitations and sorting. A 390px viewport had no horizontal overflow. The last measured 1470×919 screenshot differed at 9.29% of pixels using a 16/255 per-channel tolerance, before the final media alignment. CUA-only browser constraints prevented the vendored browser-based visual gate; the measurement used pure raster comparison. No additional checks were run after the user requested they stop.
